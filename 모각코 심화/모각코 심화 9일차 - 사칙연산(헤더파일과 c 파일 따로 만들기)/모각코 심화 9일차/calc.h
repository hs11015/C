#ifndef calc   // calc라고 하는 헤더파일이 정의되어 있는지

#define calc   // '정의되어있지 않으면 정의하겠습니다' 의미
#include <stdio.h>   // 반환 함수 return 포함

int plus(int x, int y);

int minus(int x, int y);

int multiplication(int x, int y);

int division(int x, int y);

#endif        // #ifndef 전처리문의 끝을 표시해주는 전처리문